# FLASH_HOST=localhost
# FLASH_PORT=8888
# LOG_LEVEL=INFO
RUNPOD_API_KEY=your_api_key_here

# --- SportMind ---
# Qwen2.5-7B vLLM serverless endpoint (labels/summary)
VLLM_ENDPOINT_ID=zeemah9p7zoped
VLLM_MODEL=Qwen/Qwen2.5-7B-Instruct
# Video worker URL. Local `flash dev` default below; after `flash deploy` point at the
# deployed runsync URL and set VIDEO_WORKER_KEY to your RunPod API key.
VIDEO_WORKER_URL=http://localhost:8888/video_worker/runsync
# VIDEO_WORKER_KEY=your_api_key_here
